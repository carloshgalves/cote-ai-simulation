# SIMULATION_ADMISSION_MODEL

Vazio por decisão. Ver `../README.md` e o ADR 0005, decisão 7.

Não construir enquanto as open questions `oq.admission.dimension-semantics` e
`oq.admission.placement-basis` estiverem abertas: sem elas, qualquer fórmula
seria invenção apresentada como reconstrução.
